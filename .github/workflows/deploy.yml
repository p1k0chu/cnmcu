name: Deploy project
on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    needs: natives
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Setup java
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: 25

      - name: Setup gradle
        uses: gradle/actions/setup-gradle@v5

      - name: Download natives
        uses: actions/download-artifact@v7
        with:
          path: prebuilt
          pattern: natives-*
          merge-multiple: true

      - name: Build project
        run: ./gradlew --no-daemon -P prebuilt_natives=prebuilt :build

      - name: Upload artifact
        uses: actions/upload-artifact@v6
        with:
          path: |
            build/libs/
            !build/libs/*-sources.jar
            !build/libs/*-all.jar

  natives:
    strategy:
      fail-fast: false
      matrix:
        os: [ ubuntu-latest, windows-latest, macos-latest, ubuntu-24.04-arm, windows-11-arm ]

    runs-on: ${{ matrix.os }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Setup java
        uses: actions/setup-java@v5
        with:
          distribution: microsoft
          java-version: 25

      - name: gradle build bindings
        run: ./gradlew :bindings:generateNativesSources

      - name: cmake build
        run: cd natives && mkdir -p build/bin
            && cmake -B build -S src
            && cmake --build build --config Release
            && cmake --install build --prefix build/bin

      - name: Upload natives
        uses: actions/upload-artifact@v6
        with:
          path: natives/build/bin/*
          name: natives-${{ matrix.os }}
